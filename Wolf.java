//it's an enemy, so all i need is stats, this probably doesn't need to be its own file but it's better for the project
public class Wolf extends Enemy {
    public Wolf() {
        super("Wolf", 40, 45, 5, 35);
    }
}
